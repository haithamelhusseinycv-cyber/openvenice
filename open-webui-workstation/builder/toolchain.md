# Builder Toolchain Baseline

- git, curl, wget, jq
- python, pip, venv/uv
- node, npm, pnpm
- docker, compose
- playwright, chromium
- ffmpeg, imagemagick, pandoc
- pandas, openpyxl, python-docx, matplotlib
- android sdk, adb, gradle, java (when mobile tasks require)
